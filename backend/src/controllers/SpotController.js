import Spot from '../models/Spot';

export default {
    async store(req, res) {
        console.log(req.body);
        console.log(req.file);
        // thumbnail (file), company 'Rocketseat', price 68, techs ReactJS,React-Native,Node.js
    }
}